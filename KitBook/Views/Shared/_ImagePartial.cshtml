@model KitBook.Models.ViewModels.ImageViewModel

@if (Model?.Base64 != null && Model?.Extension != null)
{
    <img class="card-img-top" src="data:image/@(Model.Extension);base64,@(Model.Base64)" alt="card image" />
}
else
{
    <img class="card-img-top" src="~/images/placeholder.png" alt="placeholder image" />
}
