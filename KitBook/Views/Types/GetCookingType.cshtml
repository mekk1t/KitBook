@model KitBook.Models.Database.Entities.Types.CookingType

@{
    ViewData["Title"] = "Детали";
}

<h1>О типах готовки "@Model.Name"</h1>

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="PutCookingType" asp-route-id="@Model.Id">Редактировать</a> |
    <a asp-action="GetCookingTypes">К списку</a>
</div>
